[package]
name = "bridge"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "26.0.1", features = ["alloc"] }

[dev-dependencies]
ed25519-dalek = "2.2.0"
soroban-sdk = { version = "26.0.1", features = ["testutils"] }
