[package]
name = "bridge"
version = "0.1.0"
edition = "2021"

[lib]
path = "lib.rs"
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "22.0.0", features = ["alloc"] }

[dev-dependencies]
ed25519-dalek = "2.2.0"
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
