[package]
name = "circuit_breaker"
version = "0.0.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "22.0.0", features = ["alloc", "testutils"] }

[lib]
name = "circuit_breaker"
path = "lib.rs"
crate-type = ["cdylib", "rlib"]
